type Eslint {
  """
  The source directory for the project.
  """
  pub source: Directory!

  """
  The base image to use.
  """
  pub baseImageAddress: String

  """
  The package manager to use.
  """
  pub packageManager: String

  new(
    ws: Workspace!,
    baseImageAddress: String = null,
    packageManager: String = null,
  ) {
    self.source = ws.directory("/")
    self.baseImageAddress = baseImageAddress
    self.packageManager = packageManager
    self
  }

  """
  Lint the source.
  """
  pub lint: Void @check {
    let node = nodejs(source, baseImageAddress, packageManager)

    node
      .exec(["eslint", "."], node.withInstallAll(node.base))
      .sync

    null
  }

  """
  Fix linting issues.
  """
  pub fix: Changeset {
    let node = nodejs(source, baseImageAddress, packageManager)
    let fixed = node
      .exec(["eslint", ".", "--fix"], node.withInstallAll(node.base))
      .directory(".")
      .withoutDirectory("node_modules")

    fixed
      .changes(source.withoutDirectory("node_modules"))
  }
}
